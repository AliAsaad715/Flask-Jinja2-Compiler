{% extends "base.html" %}

{% block title %}المنتجات - متجر المنتجات{% endblock %}

{% block content %}
<div class="page-heading">
    <div>
        <h1>قائمة المنتجات</h1>
        <p class="text-muted">صفحات ثابتة مولدة من بيانات Python وقوالب Jinja2.</p>
    </div>
    <a href="{{ url_for('add_product') }}" class="btn btn-success">إضافة منتج</a>
</div>

{% if products %}
<div class="row">
    {% for product in products %}
    <div class="col-md-6 col-lg-4">
        <article class="card product-card">
            {% if product.image %}
            <img src="{{ url_for('static', filename='uploads/' + product.image) }}"
                 class="card-img-top product-image" alt="{{ product.name }}">
            {% else %}
            <img src="{{ url_for('static', filename='product-placeholder.svg') }}"
                 class="card-img-top product-image" alt="صورة افتراضية">
            {% endif %}

            <div class="card-body">
                <h2 class="card-title product-title">{{ product.name }}</h2>
                <p class="price">{{ product.price }} ر.س</p>
                <p class="card-text text-muted">
                    {{ product.details[:100] }}{% if product.details|length > 100 %}...{% endif %}
                </p>
                <div class="action-row">
                    <a href="{{ url_for('product_detail', product_id=product.id) }}"
                       class="btn btn-primary">عرض التفاصيل</a>
                    <a href="{{ url_for('delete_product', product_id=product.id) }}"
                       class="btn btn-outline-danger">حذف</a>
                </div>
            </div>
        </article>
    </div>
    {% endfor %}
</div>
{% else %}
<div class="alert alert-info text-center">
    <h2>لا توجد منتجات حالياً</h2>
    <a href="{{ url_for('add_product') }}" class="btn btn-primary">إضافة منتج جديد</a>
</div>
{% endif %}
{% endblock %}
