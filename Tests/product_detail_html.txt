{% extends "base.html" %}

{% block title %}{{ product.name }} - متجر المنتجات{% endblock %}

{% block content %}
<nav class="breadcrumb-nav">
    <a href="{{ url_for('products_list') }}">المنتجات</a>
    <span> / </span>
    <span>{{ product.name }}</span>
</nav>

<article class="detail-card">
    <div class="row">
        <div class="col-md-6">
            {% if product.image %}
            <img src="{{ url_for('static', filename='uploads/' + product.image) }}"
                 class="detail-image" alt="{{ product.name }}">
            {% else %}
            <img src="{{ url_for('static', filename='product-placeholder.svg') }}"
                 class="detail-image" alt="صورة افتراضية">
            {% endif %}
        </div>

        <div class="col-md-6 detail-content">
            <span class="product-number">المنتج رقم {{ product.id }}</span>
            <h1>{{ product.name }}</h1>
            <p class="price detail-price">{{ product.price }} ر.س</p>
            <h2>تفاصيل المنتج</h2>
            <p class="lead">{{ product.details }}</p>

            <div class="action-row">
                <a href="{{ url_for('products_list') }}" class="btn btn-outline-secondary">العودة للقائمة</a>
                <a href="{{ url_for('delete_product', product_id=product.id) }}"
                   class="btn btn-danger">حذف المنتج</a>
            </div>
        </div>
    </div>
</article>
{% endblock %}
